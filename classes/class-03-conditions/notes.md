# Class 03: Input and Conditions

## Taking Input

The `input()` function is used to take information from the user.

```python
name = input("Enter your name: ")
```

## Conditions

Conditions help Python make decisions.

```python
age = 18

if age >= 18:
    print("You are an adult")
else:
    print("You are not an adult")
```

## Comparison Operators

- `==`: equal to
- `!=`: not equal to
- `>`: greater than
- `<`: less than
- `>=`: greater than or equal to
- `<=`: less than or equal to

## Practice

1. Take the user's name as input.
2. Take the user's age as input.
3. Check if the user is 18 or older.
